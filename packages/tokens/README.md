# @dropblocs/tokens

The shared token layer every block consumes: color, type, radius, spacing and motion scale (named easings + duration scale).

Theme once, all blocks follow. Values here are placeholders until the brand pass.

Usage in a Tailwind v4 project:

```css
@import "tailwindcss";
@import "@dropblocs/tokens/tokens.css";
```
